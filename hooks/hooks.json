{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ ! -f .mcp.json ] || ! grep -q snipara .mcp.json 2>/dev/null; then echo '[Snipara] Not configured. Run /snipara:quickstart to set up (free, 100 queries/month, no credit card).'; else echo 'Snipara: Ready.'; fi"
          }
        ]
      }
    ]
  }
}
